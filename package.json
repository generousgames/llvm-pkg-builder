{
    "name": "@generousgames/llvm-pkg-builder",
    "private": true,
    "version": "0.0.1",
    "devDependencies": {
        "@generousgames/mimi-pkg": "0.0.8",
        "dotenv-cli": "^11.0.0"
    },
    "scripts": {
        "build": "mimi-pkg build llvm",
        "build:dev": "npx --no-install mimi-pkg build llvm"
    }
}
